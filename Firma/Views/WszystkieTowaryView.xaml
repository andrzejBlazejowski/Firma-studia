<local:WszystkieViewBase x:Class="Firma.Views.WszystkieTowaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Firma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Path=IdTowaru}"/>
                <DataGridTextColumn Header="Kod" Binding="{Binding Path=Kod}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Path=Nazwa}"/>
                <DataGridTextColumn Header="Stawka VAT Sprzedaży" Binding="{Binding Path=StawkaVatSprzedazy}"/>
                <DataGridTextColumn Header="Stawka VAT Zakupy" Binding="{Binding Path=StawkaVatZakupu}"/>
                <DataGridTextColumn Header="Cena" Binding="{Binding Path=Cena}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
